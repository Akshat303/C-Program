// C program to addition of two number

#include <stdio.h>
int main()
{
    int a, b, c;
    printf("Enter the value of A = ");
    scanf("%d", &a);
    printf("Enter the value of  B = ");
    scanf("%d", &b);
    c = a + b;
    printf("sum of A & B  is = %d", c);
    return 0;
}